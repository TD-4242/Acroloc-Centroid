# Subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# Local extraction scratch for skill authoring (e.g. pdftotext output)
scratchpad/

# Centroid mpucomp scratch outputs (compile.sh writes to a temp dir, but these
# appear if mpucomp is run by hand in the repo root)
plc.out
mpu.dbg
mpu.sym
xplc.sym
mpu-stats.txt

# Compiled PLC binary for this machine's live controller - build output, not
# source. Anchored to repo root so it doesn't affect the docs/official/
# reference example projects, which intentionally check theirs in.
/mpu.plc
